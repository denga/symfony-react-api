nelmio_api_doc:
    documentation:
        info:
            title: Orders API
            description: API for creating and listing orders.
            version: 1.0.0

    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
